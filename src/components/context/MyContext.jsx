import React from 'react';

const MyContext = React.createContext();


export default MyContext;

// used this context for keeping store of the information submitted by user from the reservation but redux is better tbh
